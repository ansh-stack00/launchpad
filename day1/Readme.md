# 📰 Blog Page – Semantic HTML Project  

## 📘 Overview  
This project demonstrates my understanding of **semantic HTML5 structure** and **accessible page design** without using any CSS.  
The goal was to replicate a basic blog page layout using **only semantic HTML elements** — no `<div>` tags, no inline styling, and no CSS files.  

---

## 🎯 Learning Outcomes  
- Understand and apply correct **HTML page structure** (`<head>`, `<header>`, `<main>`, `<footer>`, etc.)  
- Use **semantic tags** like `<article>`, `<section>`, `<nav>`, `<header>`, `<aside>`, `<footer>` appropriately  
- Create **forms and media** elements (e.g., `<form>`, `<input>`, `<video>`, `<audio>`, `<img>`)  
- Implement **basic accessibility** best practices using:
  - `alt` attributes for images  
  - `aria-label` for assistive technologies  
  - `tabindex` for keyboard navigation  
- Practice **responsive and meaningful HTML layout scaffolding** without relying on CSS  

---


---

## 🧩 Features Implemented  

### 📰 Semantic Layout  
- **Header**: Contains the blog title and navigation menu  
- **Nav**: Semantic navigation with links for different blog sections  
- **Main**: Wraps blog content including multiple `<article>` elements  
- **Article**: Each post includes a header, author section, content, and footer  
- **Aside**: Includes related posts or advertisement placeholders  
- **Footer**: Contains copyright and contact details  

### 🧠 Accessibility  
- Used `alt` text for all images  
- Included `aria-label` for nav and form sections  
- Added `tabindex` to important interactive elements for keyboard access  

### 🧾 Forms & Media  
- Simple **contact form** with `<input>`, `<textarea>`, `<select>`, and validation attributes (`required`, `type="email"`)  
- Embedded **image and video** media using `<img>` and `<iframe>`  

---

## 🧰 Technologies Used  
- **HTML5 only** (no CSS or JS)  
- **Semantic tags** for structure and meaning  
- **Accessible attributes** (`alt`, `aria-*`, `tabindex`)  

---

## 🧪 Exercise Reference  
**UI Inspiration:**  
[Blog Layout Reference Image](https://designhooks.com/wp-content/uploads/2020/05/full-template-551-stand-blog-scaled.jpg)

---

## 📄 Deliverables  
- `blog.html` — Blog layout built entirely using **semantic HTML**  
- `README.md` — Project documentation (this file)  

---

## 🚀 Key Takeaways  
- Proper use of **semantic elements** makes your HTML more readable, SEO-friendly, and accessible.  
- Even without CSS, clear structure and tag choice can visually separate content meaningfully.  
- Accessibility and structure should always come **before styling**.  


